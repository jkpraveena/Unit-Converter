# Use official Java image
FROM openjdk:11-jre-slim

# Set working directory inside the container
WORKDIR /app

# Copy the built jar file
COPY target/unit-converter-0.0.1-SNAPSHOT.jar app.jar

# Run the app
ENTRYPOINT ["java", "-jar", "app.jar"]
